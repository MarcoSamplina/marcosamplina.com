/**
 * El sitemap se sirve desde app/sitemap.xml/route.ts para incluir
 * la hoja de estilo XSL (vista tipo Yoast) y lastmod real por post.
 * No exportar default aquí para no duplicar la ruta /sitemap.xml.
 */
